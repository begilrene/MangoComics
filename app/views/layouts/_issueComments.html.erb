   <% provide(:title, @iss.issueID) %>
    <div class = "container">
      <div class = "row">
        <div id: "comments">
          <div class="col-sm-12">
            <% IComment.where(issueID: @iss.issueID ).each do |iComments| %>
            <p> <%=iComments.displayName%> Date: <%=iComments.date%> <br /></p>
            <p id = "comments"> <%=iComments.body%></p>
            <% end %>
          </div>
        </div>
      </div>
    </div>
