   <% provide(:title, @iss.issueID) %>
    <div class = "container">
      <div class = "row">
        <div id: "comments">
          <div class="col-sm-12">
            <% @iss.IComments.each do |iComments| %>
            <p> <%=iComments.display%> Date: <%=iComments.date%> <br /></p>
            <p id = "comments"> <%=iComments.body%></p>
            <% end %>
          </div>
        </div>
      </div>
    </div>
