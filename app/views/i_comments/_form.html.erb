      <% if logged_in? %>
      
        <%= form_for ([@iss,@iss.IComments.build]) do |f| %>
          <%= render 'shared/error_messages' %>

          <%= f.label :body, "Comment" %>
          <%= f.text_area :body %>
          

          <%= f.submit "Post", class: "btn btn-large btn-primary" %>
        <% end %>
      <% end %>