<div class = "container">   
      <% if logged_in? %>
      
        <%= form_for ([@user,@comm.build]) do |f| %>
          <%= render 'shared/error_messages' %>

          <%= f.label :body, "Comment" %>
          <%= f.text_area :body %>
          

          <%= f.submit "Post", class: "btn btn-small btn-primary" %>
        <% end %>
      <% end %>
</div>